- don't use AI vocabulary and paterns, for example never use "--"
- never use magic values in Tailwind/Uniwind classes (no arbitrary values like `text-[28px]`, `py-[5]`, `bg-[#fff]`, etc.). Always use the default design tokens already set up in the project, for example `text-xl`, `text-foreground`, `bg-surface`, `bg-background`, `text-muted`, etc.

