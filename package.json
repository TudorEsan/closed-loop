{
  "name": "closed-loop",
  "private": true,
  "description": "Run the whole closed-loop stack (backend, admin, softpos, stripe webhook listener) at once.",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names backend,admin,softpos,stripe --prefix-colors blue,magenta,green,yellow \"npm run dev:backend\" \"npm run dev:admin\" \"npm run dev:softpos\" \"npm run dev:stripe\"",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:admin": "npm --prefix admin-portal run dev",
    "dev:softpos": "npm --prefix softpos run start",
    "dev:stripe": "stripe listen --forward-to http://localhost:3000/api/v1/payments/webhook"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "expo": "^55.0.17",
    "react": "19.2.0",
    "react-native": "0.83.6"
  },
  "version": "1.0.0"
}
