# Stripe publishable key (test mode). Safe to expose, public half of the pair.
# Must match the STRIPE_PUBLISHABLE_KEY used by the backend.
# Grab it from https://dashboard.stripe.com/test/apikeys
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
